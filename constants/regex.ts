/**
 * Regular expressions shared across the app.
 * Centralized here to avoid duplication and inconsistencies.
 *
 * Examples:
 * - Email validation
 * - Slug validation
 * - Password strength
 */

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
