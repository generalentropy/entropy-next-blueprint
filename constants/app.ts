/**
 * Application-level constants.
 * These values are not environment-dependent.
 *
 * Examples:
 * - Application name
 * - Default language
 * - UI-related keys (e.g. localStorage keys)
 * - Feature flags (static, not controlled by ENV)
 */

export const APP_NAME = "";
export const DEFAULT_LANGUAGE = "";
export const THEME_STORAGE_KEY = "theme-storage";
